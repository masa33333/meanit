# MeanIt — Claude Code 引き継ぎドキュメント

このファイルは Claude Code がセッション開始時に自動で読む。プロジェクトの設計思想・確定事項・触ってはいけない原則をここに集約してある。コードを変える前にここを読むこと。

---

## これは何か

**意図を通す実技練習場。** めちゃくちゃな英語で話す → 会話相手AIが意図を汲んで温かく反応 → 相棒コーチAIが「こう聞こえた／本当はこう言いたかった？／ズレ1点／自然な言い方」を日本語で返す → 失敗ログに意図ごと蓄積。

一言で：**「向こうに会話相手、隣に指導官、詰まったら日本語、会話のたびに失敗ログが資産になる」英会話教習所。**

対象は英語学習アプリだが、本質は出力訓練。「正しい英語を作る」ではなく「伝えたい意図を英語で表現しきる」訓練。

---

## 絶対に守る原則（invariants）

これらを崩す変更は入れない。崩す必要が出たらコードを書く前に必ず確認を取る。

1. **失敗時も学習者を冷たく突き放さない。** API が落ちても無機質なエラーは出さず、会話相手がキャラのまま自然に聞き返す。失敗の恐怖を強化する一切の挙動を禁ずる（このアプリの存在意義そのもの）。
2. **音声認識（STT）は入力手段であって、発音は採点しない。** 発音採点を入れた瞬間に「失敗の恐怖」が戻りコアと衝突する。発音はスコープ外（それは別プロダクト RETENTION! の領域）。
3. **APIキーとプロンプトは絶対にクライアントに出さない。** プロンプト＝methodology＝IP。`server/` 配下のみ。
4. **コアは意図伝達。** 「速く何か言えば勝ち」のような意図精度を犠牲にする報酬設計を本編ループに混ぜない（沈黙対策が要るなら独立モードに隔離）。
5. **現MVPの対象は中級/masa型ペルソナ**（英語は出るが意図とズレる層）。完全初級者向けの足場掛け・前向きヘルプは v2 まで入れない。スコープを広げる前に確認。

---

## コア体験：意図レスキュー・ループ

1. 会話相手AI（🌍 Friend）が自己開示トピックで質問（週末・趣味・出身など）
2. 学習者がめちゃくちゃ英語で答える（テキスト or 音声）
3. 会話相手AIが**意図を汲んで温かく反応**（伝わった喜び）＋次の質問。文法は直さない
4. 同時に相棒コーチAI（🎒）が日本語で：
   - **こう聞こえた**（字義通りの意味）
   - **本当はこう言いたかった？**（推測意図／はい・もう少し違う）。「もう少し違う」→日本語で真意を入力→それに合わせ再生成
   - **ズレ**（日本語の発想が出た箇所を1点だけ）
   - **自然な言い方**（部品。高頻度・非スラング）
5. **失敗ログ**に「意図＋言ったこと＋自然な言い方」を自動記録

加えて各学習者ターンに「**言いたかったのに言えなかった**」ボタン（前向きの穴の捕獲。答えは渡さず記録だけ）。

---

## なぜ特別か（差別化）

- だいじろー（発音インフルエンサー）のアプリ＝「伝わった＋自然な言い換え」止まり。発音認識が軸。
- 普通のオンライン英会話＝会話相手しかいない。学習者が口から出した英語にしか反応できず、「言いたかったこと」が永久に置き去り。
- **MeanIt の核は「後ろ向きの意図レスキュー」**：言えたつもりが意図とズレている瞬間を、母語で掴んで構造的に見せる。これは誰もやっていない。ここが死ぬとプロジェクトが死ぬ。

SLA的根拠：Swain の出力仮説／noticing the gap、Long のリキャスト、Lewis のレキシカルアプローチ。意図確認に母語を使うのは妥協ではなく設計上の正解（母語が最速・最正確）。

---

## MVPスコープ

**入っているもの：** 意図レスキュー・ループ／意図確認（はい・もう少し違う）／ズレ1点／自然な言い方（部品）／失敗ログ／「言いたかったのに言えなかった」ボタン／会話相手の英語レベル切替（やさしめ・ふつう・リアル、既定やさしめ）／音声（Whisper）。

**意図的に捨てた／後回し（着手したくなったら順番はこの辺）：**
1. メモ＝武器庫（部品の収集・タグ付け・コレクション化）
2. リプレイ（同じ会話の再挑戦／🟥詰まった・🟨頼った・🟩自力 の色分け可視化）
3. 補助輪が外れる難易度自動上昇（上達に応じ会話相手の英語が自然になる）
4. 前向きヘルプ（「ここでこう言いたい」を3段階で救助）← 初級者展開 v2 の主砲
5. 多言語展開（母語別の失敗マップ。Ondoku とシナジー）
6. 沈黙ゲーム（即応訓練。本編とは別モードに隔離）

捨てた理由は一貫して「コア（意図推測の精度と感情的破壊力）を汚さず先に検証するため」。コアは検証済み。乗せる順は上記だが、各機能の着手前に設計をチャット側で詰める。

---

## アーキテクチャ

```
ブラウザ(React) ─POST /api/chat──────▶ サーバー ─▶ Anthropic（会話・コーチ）
  ↑ キーもプロンプトも見えない        ─POST /api/transcribe─▶ OpenAI Whisper（音声→テキスト）
                                      ↑ ANTHROPIC_API_KEY / OPENAI_API_KEY を保持
```

- プロンプトは `server/prompts.ts` のみ。クライアントへ配信しない（IP保護＋プロキシ悪用防止）。
- dev：`vite.config.ts` のプラグインが `/api/*` を Vite dev サーバーに同居させる（`npm run dev` 一発）。
- prod：`api/*.ts`（Vercel サーバーレス）が同じ `server/*.ts` を共有。
- 音声入力は **Whisper 方式**。ブラウザ内蔵の Web Speech Recognition は Brave 等で動かないため不使用。録音（MediaRecorder）→ base64 → `/api/transcribe` → Whisper。

---

## テストで確定した実装上の決定（再発させない）

過去に実際にハマって直した点。理由ごと残す。

- **IME**：日本語入力欄の Enter は `e.nativeEvent.isComposing` でガード（変換確定のEnterで誤送信しない）。
- **頑健なJSONパーサ**：コーチ出力は単一JSON。文字列値に生の改行が入ると `JSON.parse` が壊れた事例あり。対策＝(1) 2案目は改行で足さず `naturalAlt` 別フィールド、(2) パーサは厳密parse失敗時に正規表現で各フィールドを拾う、(3) API は自動1回リトライ。`server/handler.ts` 参照。
- **会話相手の英語レベル自動追従＋イディオム封印**：相手がイディオムを多用すると「解読」に認知資源を奪われ出力訓練が薄まる。`easy` は NGSL コア中心・短文・イディオム禁止。`server/prompts.ts` の `LEVELS` 参照。
- **オープンクエスチョン優先**：会話相手は What/How/Why 系を基本に。yes/no で終わるクローズドは原則封印（出力量を最大化するため）。
- **Whisper 設定**：`language=en` `temperature=0`。学習者の英語を綺麗に補正しすぎず、なるべくそのまま文字起こしさせるため（補正されすぎるとコーチの指摘が薄くなる）。`server/transcribe.ts` 参照。
- **音声UXは「録音→停止→変換」の二段階**（リアルタイム逐次ではない）。

---

## ファイル構成

```
api/chat.ts            本番サーバーレス：会話・コーチ
api/transcribe.ts      本番サーバーレス：音声→テキスト
server/
  prompts.ts           SYSTEM / LEVELS / MODEL（← methodology。育てる場所）
  handler.ts           Anthropic 呼び出し＋頑健パーサ（dev/prod 共有）
  transcribe.ts        OpenAI Whisper 呼び出し（dev/prod 共有）
src/
  App.tsx              画面本体・状態管理・録音制御
  components/
    CoachCard.tsx      相棒コーチのカード
    LogPanel.tsx       失敗ログのパネル
  lib/
    api.ts             /api/* を叩くクライアント（sendTurn/sendCorrection/transcribe）
    speech.ts          TTS（speak）＋録音ヘルパ（recorderSupported/blobToBase64）
  types.ts
vite.config.ts         dev で /api/* を同居させるプラグイン
tailwind.config.js     テーマ（色・フォント・アニメ）
```

---

## 技術スタック・セットアップ・デプロイ

- React 19 + TypeScript + Vite 6 + Tailwind 3、@anthropic-ai/sdk。音声は OpenAI Whisper（REST 直叩き）。
- セットアップ：`npm install` → `cp .env.example .env`（2つのキーを設定）→ `npm run dev`。
- 環境変数：`ANTHROPIC_API_KEY`（会話・コーチ）/ `OPENAI_API_KEY`（Whisper）。**`VITE_` を付けない**（付けるとクライアントに漏れる）。
- デプロイ：Vercel。GitHub push で自動デプロイ。env を Vercel 側にも設定。
- build は `vite build` のみ（型チェックは別途 `npm run typecheck`）。
- モデル変更は `server/prompts.ts` の `MODEL`。

---

## 作業の進め方

- **設計・方針の判断はチャット側（claude.ai）で詰める** → 決まったことを Claude Code で実装、の往復運用。
- 上記 invariants に関わる変更、捨てた機能の復活、スコープ拡大は、コードを書く前に必ずチャット側で確認を取る。
- 小さな実装・バグ修正・リファクタは Claude Code 側で進めてよい。
